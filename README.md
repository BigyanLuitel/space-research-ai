# space-research-ai
AI-powered Space Research Assistant built using Django, RAG architecture, and Generative AI to analyze satellite and space-related documents through intelligent question answering.

# create a virtual environment
python -m venv .venv
load the env
./.venv/Scripts/activate

# install requiremnts
pip install -r requirements.txt

# run server
python manage.py runserver

html file
|-> inside templates/RAG

css & JS
|-> inside static 